server:
  port: 7001

eureka:
  instance:
    hostname: eurekapojer
  #    hostname: eureka7001.com
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:7002/eureka/